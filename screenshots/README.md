# Screenshot Placeholders

Add final execution screenshots here before submission.

Suggested screenshots:

- `docker/`: Docker image build and container run output.
- `kubernetes/`: `kubectl get pods`, `kubectl get svc`, and pod logs.
- `github_actions/`: successful GitHub Actions workflow run.
