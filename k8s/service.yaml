apiVersion: v1
kind: Service
metadata:
  name: smart-education-analytics
  labels:
    app: smart-education-analytics
spec:
  type: ClusterIP
  selector:
    app: smart-education-analytics
  ports:
    - name: notebook-runner
      port: 8080
      targetPort: 8080
